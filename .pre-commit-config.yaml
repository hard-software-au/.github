# ============================================================================
# This file is auto-generated by bootstrap-hooks.sh
# DO NOT EDIT DIRECTLY
#
# To regenerate, run: ./scripts/bootstrap-hooks.sh <profiles>
# Management: Controlled by .github repo (https://github.com/.../.github)
# ============================================================================

# Profiles: baseline
fail_fast: true
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: no-commit-to-branch
        name: not working on a protected branch
        args: [--branch, main, --branch, master]
        stages: [pre-commit]

  # Prettier — code formatting (managed as a local node hook).
  # Scoped by caller: each repo's generated .pre-commit-config.yaml specifies files: pattern.
  - repo: local
    hooks:
      - id: prettier
        name: prettier (code formatter)
        # files: provided by caller (e.g., ^src/, ^app/)
        language: node
        entry: prettier --check
        args: [--ignore-unknown]
        additional_dependencies:
          - prettier
        types_or: [yaml, json, markdown, javascript, ts, css, html]
        stages: [pre-commit]

  # Secret detection — local script wrapper (repo-specific baseline).
  - repo: local
    hooks:
      - id: detect-secrets
        name: detect-secrets (secret scanning)
        language: python
        entry: detect-secrets-hook
        additional_dependencies:
          - detect-secrets
        args: ["--baseline", ".secrets.baseline"]
        exclude: \.secrets\.baseline
        stages: [pre-commit]

      - id: gitleaks
        name: gitleaks (credential leak detection)
        language: system
        entry: gitleaks protect --staged
        pass_filenames: false
        stages: [pre-commit]

      - id: commit-msg-check
        name: commit message convention
        language: script
        entry: scripts/commit-msg-check.sh
        stages: [commit-msg]
